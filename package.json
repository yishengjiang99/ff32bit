{
  "name": "ff32bit",
  "version": "1.0.0",
  "description": "Stream a WAV from fetch into an AudioWorklet.",
  "license": "ISC",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/stage-pages.cjs",
    "preview": "vite preview",
    "playtest": "node playtest.cjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}